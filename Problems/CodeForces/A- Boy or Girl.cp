//problem link: https://codeforces.com/contest/236/problem/A

#include<iostream>
#include <stack>
#include <vector>
#include <queue>
#include <cstdio>
#include<set>
#include<map>
using namespace std;
int main() {
ios_base::sync_with_stdio(false);
cin.tie(NULL);
    string s;
    cin>>s;
    set<char> t(begin(s), end(s));
    if(st.size()%2==0) cout<<"CHAT WITH HER!\n";
    else cout<<"IGNORE HIM!\n";
    return 0;
}
